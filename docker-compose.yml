services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: german-test-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Europe/Berlin
    volumes:
      - bot_data:/app/data

volumes:
  bot_data:
