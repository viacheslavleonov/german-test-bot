services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: german-test-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot_data:/app/data

volumes:
  bot_data:
